/**
 * Created by LittleKolio on 9/24/2016.
 */
function multiplyNumbers([num1, num2]) {
    return num1 * num2;
}
/**
function multiplyNumbers(input) {
    let multiply = input[0] * input[1];
    return multiply;
}
 */